{
  "name": "scotti-indexer",
  "private": true,
  "type": "module",
  "description": "Scotti House Module indexer — a standalone off-chain service that samples machine share prices and indexes the spin feed (each spin recomputed at ingest), served over a small read-only JSON API. Reuses the on-chain decoders + house-math from ../scripts (never re-derives layouts). SQLite via node:sqlite, HTTP via node:http — no external services.",
  "scripts": {
    "ingest": "node src/cli.ts ingest",
    "serve": "node src/cli.ts serve",
    "dev": "node src/cli.ts dev",
    "typecheck": "tsc --noEmit",
    "test": "node --test test/*.test.ts"
  },
  "dependencies": {
    "@solana/web3.js": "^1.98.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0"
  }
}
