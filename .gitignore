# Rust / Anchor
target/
**/*.rs.bk

# Anchor
.anchor/
.surfpool/
test-ledger/
.DS_Store

# Node
node_modules/
dist/
.env
.env.*
!.env.example

# Keypairs — NEVER commit these
*.json
!package.json
!tsconfig.json
!Anchor.toml
!scripts/package-lock.json
!app/package-lock.json
!indexer/package-lock.json
# Live devnet spin records (pubkeys, signatures, revealed randomness — no secrets)
!scripts/spins/*.json
# Indexer test fixtures — captured real devnet txs (public data), needed by the tests
!indexer/test/fixtures/*.json

# Program identity keypairs — the on-chain program IDs live here; NEVER commit.
# Backed up out of target/deploy/ so `cargo clean` cannot destroy them.
keys/
