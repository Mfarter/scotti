# Solana devnet RPC endpoint. The public endpoint works but is rate-limited;
# a free Helius/QuickNode/Triton devnet URL is smoother. NEVER commit your .env.
VITE_RPC_URL=https://api.devnet.solana.com
